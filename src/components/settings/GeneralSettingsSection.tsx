export function GeneralSettingsSection() {
  return (
    <div className="space-y-8 py-8">
      <section className="pb-2">
        <h2 className="text-xl font-medium mb-0.5">General</h2>
        <p className="text-sm text-text-muted">
          Aoroza is a minimal Markdown editor. Your files are stored wherever you choose on disk — no vault, no library, no lock-in.
        </p>
      </section>
    </div>
  );
}
